# Astro Runtime includes the following pre-installed providers packages: https://www.astronomer.io/docs/astro/runtime-image-architecture#provider-packages

# Core dependencies
pandas>=1.5.0
requests>=2.28.0
python-dotenv>=0.19.0
numpy>=1.21.0
pyarrow>=14.0.0

# Azure dependencies - Data Lake Gen2
azure-storage-file-datalake>=12.8.0
azure-identity>=1.12.0

# Azure dependencies - Data Factory Management
azure-mgmt-datafactory>=3.0.0
azure-mgmt-resource>=23.0.0

# Azure dependencies - SQL Database (optional for direct loading)
pyodbc>=4.0.39
# Note: sqlalchemy version managed by Astro Runtime (compatible with Airflow)

# Additional Airflow providers (if not included in Astro Runtime)
apache-airflow-providers-postgres>=5.6.0

# Development dependencies
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0
